<%= form_for(@newsletter) do |f| %>
  <% if @newsletter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@newsletter.errors.count, "error") %> prohibited this newsletter from being saved:</h2>

      <ul>
      <% @newsletter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= render 'markdown/markdown' %>

  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title, required: true %>
  </div>
  <div class="field">
    <%= f.label :subtitle %>
    <%= f.text_field :subtitle, required: true %>
  </div>
    <div class="field">
    <%= f.label :date %>
    <%= f.date_field :date, required: true %>
  </div>
  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body, class: "bigtext", required: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
